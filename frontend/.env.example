# Public URL of your Railway (or other) Express API — no trailing slash.
# Required at build time for Vite so the client bundle knows where to call the API.
# VITE_API_URL=https://your-backend-service.up.railway.app
